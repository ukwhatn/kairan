# kairan

Local MCP server that circulates agent-generated markdown/html documents to your browser.

Claude Code / Codex などの agent から markdown / HTML を tool call で投げると、ブラウザで閲覧できる回覧板。何個の agent から接続されても表示サーバーは1つ・port は1つ。

（実装中。使い方は実装完了後に記載）
